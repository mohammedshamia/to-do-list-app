<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.todo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TODO">
        <activity android:name=".Pages.EditTaskPage"></activity>
        <activity android:name=".Pages.ChoiceClass" />
        <activity android:name=".Items.TaskItem" />
        <activity android:name=".Items.InformationTask" />
        <activity android:name=".Pages.Result" />
        <activity android:name=".Pages.TaskChoicesClass" />
        <activity android:name=".Items.ListOfTasks" />
        <activity android:name=".Authentication.LoginPage" />
        <activity android:name=".Authentication.RegisterPage" />
        <activity android:name=".Pages.BaseScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>